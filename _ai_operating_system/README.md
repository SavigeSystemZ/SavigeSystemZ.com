# AI Operating System Layer

This directory stores planning, execution prompts, risk tracking, and handoff context separately from website runtime code.

## Rules
- Do not couple these files to app runtime imports.
- Keep milestone state current after each implementation pass.
- Treat docs in `docs/` as product source-of-truth.
