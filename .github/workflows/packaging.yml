name: packaging

on:
  workflow_dispatch:

jobs:
  linux-packages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: chmod +x installer/scripts/build-packages.sh
      - run: installer/scripts/build-packages.sh
